<div class="gradient-overlay fixed top-0 left-0 w-full h-full pointer-events-none"></div>
<style>
    .gradient-overlay {
        background: radial-gradient(
            circle at center,
            rgba(0, 0, 0, 0) 0%,
            rgba(0, 0, 0, 0.08) 50%,
            rgba(0, 0, 0, 0.12) 85%,
            rgba(0, 0, 0, 0.15) 100%
        );
        opacity: 0.9;
        animation: pulse 8s ease-in-out infinite;
    }
    @keyframes pulse {
        0%, 100% {
            transform: scale(1);
            opacity: 0.9;
        }
        50% {
            transform: scale(1.04);
            opacity: 0.8;
        }
    }
</style>